fun main() {
    val invitation = readLine().toBoolean() // read other value in the same way
    val invitation2 = readLine().toBoolean() // read other value in the same way
    println(invitation && invitation2)

}